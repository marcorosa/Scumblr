version: "2"
services:
  scumblr:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - "postgres"
      - "redis"
    links:
      - "postgres"
      - "redis"
    environment:
      - REDIS_URL=redis://redis:6379
    env_file:
      - scumblr-vars.env
    entrypoint:
      - /docker-entrypoint.sh
      - init
  postgres:
    image: postgres:11.5
    env_file:
      - postgres.env
  redis:
    image: redis:latest
